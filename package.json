{
  "name": "bano-wyderka",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "services/*"
  ],
  "scripts": {
    "dev": "bun --filter website run dev",
    "build": "bun --filter website run build",
    "lint": "bun --filter website run lint",
    "start": "bun --filter website run start",
    "test": "bun test"
  },
  "ignoreScripts": [
    "sharp",
    "unrs-resolver"
  ],
  "trustedDependencies": [
    "sharp",
    "unrs-resolver"
  ],
  "devDependencies": {
    "@types/node": "^25.2.0",
    "bun-types": "^1.3.8",
    "yaml": "^2.8.2"
  }
}
